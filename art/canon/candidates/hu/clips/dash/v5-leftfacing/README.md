v5: rolled from UNIFORM left-facing pins (mirrored-k1 -> canon k5 -> mirrored-k1)
after the user's finding that mirroring mixed-pin clips preserves the internal
hand swap. Left-facing native = final player convention (orientation-flip spec
rev). Supersedes v3 (right-facing, mixed pins) and v4-mirrored (mixed preserved).
