Light-attack clip harvest (2026-07-08): Seedance via pixelforge-sprite
animate-video, sequence mode k1->k2->k1 (canon pins), 4s slow-mo resampled to
10 frames (windup 3 / active 4 / recovery 3), pixelized 256/vinik24,
pose-aware normalization (bbox-height lerped k1<->k2 by aspect; mass-checked
vs canon ~1.0). Seedance baked a gold tip-spark into active frames - keep or
strip at install (watch S8a gold = parryable-cue collision). Gate-2 pending;
in-game scale verify at install per judge-art-size-overall-in-game.
