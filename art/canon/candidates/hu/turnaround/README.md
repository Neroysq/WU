Hu turnaround candidates (2026-07-08): sprite-extractor animate on the approved
PersonaGen-derived master (HU.md §9), actions idle+walk-cycle, 256/vinik24,
0 off-palette. Awaiting user pick-gate; on approval -> art/canon/hu/ + manifest.

REJECTED (2026-07-08 user gate): the generated idle is a TALKING/pre-combat pose,
not a combat idle. Corrected process (user directive):
  1. First approve f1 = COMBAT IDLE keyframe (sword drawn, ready stance) - Gate 1.
  2. Then generate walk/attack/jump/dash/block with f1 pinned as start AND end
     pose (+ key mid frames if needed) - the established idle-pinned workflow.
The relaxed idle/walk sheets here may be reused later as out-of-combat/dialogue
poses (仙麓 scenes), not combat.
